@model IEnumerable<WebApplication1.Models.Onderdeel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OnderdeelName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OnderdeelQuantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OnderdeelName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OnderdeelQuantity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OnderdeelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OnderdeelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OnderdeelId })
        </td>
    </tr>
}

</table>
